# hs: WordPress login brute force (templates/fail2ban/wordpress-filter.conf)
[Definition]
allowipv6 = auto
failregex = ^<HOST> .* "POST .*wp-login\.php
            ^<HOST> .* "POST .*xmlrpc\.php
ignoreregex =
